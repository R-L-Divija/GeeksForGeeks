#include <iostream>
using namespace std;

int main() {
    // Take a as input

    // find it is positive, negative or zero
     int a;
    cin>>a;
    if(a==0)
    {
        cout<<"Zero";
    }
    if(a<0)
    {
        cout<<"Negative";
    }
    if(a>0)
    {
        cout<<"Positive";
    }

    return 0;
    return 0;
}